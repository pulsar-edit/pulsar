# Build output
dist/
binaries/

# Vendored / third-party (not our source)
vendor/
# Public-domain word list from Project Gutenberg, not hand-maintained source.
spec/text-buffer-helpers/words.js

# Generated / lockfiles / minified
yarn.lock
*.min.js
# Regenerated by each autocomplete package's `update/` script; formatting it
# here would be undone on the next `yarn update` and re-break the gate.
**/completions.json

# Test fixtures — exact content is asserted by specs; must not be reformatted
**/fixtures/
